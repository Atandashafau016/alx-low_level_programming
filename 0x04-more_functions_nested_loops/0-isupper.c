#include "main.h"
/**
 *_isupper - checks for uppercase character
 *@c: the character
 *Return: 1 if letter is uppercase and 0 if not
 */
int_isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
else
{
return (0);
}
}
