#include "main.h"
/**
 *_mecpy - copies memory area
 *@dest: the adress to copy to
 *@src: the adress i'm copying from
 *@n: the third value
 *Return: dest
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
unsigned int i = 0;
for (i = 0; i < n; i++)
{
dest[i] = src[i];
}
return (dest);
}
